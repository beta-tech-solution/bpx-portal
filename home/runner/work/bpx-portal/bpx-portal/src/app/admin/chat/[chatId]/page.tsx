
// This file is now empty because its contents have been moved to page.web.tsx.
// The mobile build will not include the .web.tsx file, thus avoiding the build error.
// The web build will include it.
export default function AdminChatPage() {
  return null;
}
